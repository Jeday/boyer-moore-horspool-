#ifndef SUBSTR_H
#define SUBSTR_H

#include <string>
#include <vector>
#include <cassert>


std::vector<int> * search(const std::string& pattern,const std::string& str, int alph_size = 256);

#endif // SUBSTR_H
